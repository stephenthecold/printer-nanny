# Generated by deploy/install.sh — edit this directly and run
# `docker compose restart caddy` to apply changes, or run install.sh with
# `--reset-caddy` to regenerate from this template.

{
__GLOBAL_OPTIONS__
}

__SITE__ {
    reverse_proxy api:8000
}
